return {
	"mg979/vim-visual-multi",
	event = "VeryLazy",
	config = function()
		-- Solo redefinir los mapeos que quieres cambiar
		-- vim.g.VM_maps = {
		-- 	["Add Cursor Down"] = "<A-Down>", -- Agregar cursor hacia abajo con Alt + ↓
		-- 	["Add Cursor Up"] = "<A-Up>", -- Agregar cursor hacia arriba con Alt + ↑
		-- }
	end,
}
